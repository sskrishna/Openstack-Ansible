mysql -u root -padmin -e "GRANT ALL PRIVILEGES ON {{ db.db_name }}.* TO '{{ db.db_name }}'@'localhost' IDENTIFIED BY '{{ db.openstack_dbpass }}';"

mysql -u root -padmin -e "GRANT ALL PRIVILEGES ON {{ db.db_name }}.* TO '{{ db.db_name }}'@'%' IDENTIFIED BY '{{ db.openstack_dbpass }}';"
